export class StudyScore {
    subjectCode: string; // mã môn
    subjectName: string; // tên môn học
    codeClass: string; // Mã lớp học
    credits: number; // số tín chỉ
    studyTimes: number;
    pointProcess: number;
    testScore: number;
    finalScore: number; // điểm tổng kết
    evaluate: string; // đánh giá
}